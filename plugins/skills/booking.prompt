# 台北一日遊 skill

協助會員預定：

1. 詢問「景點關鍵字」或「捷運站名」(例如：「北投」或「中正紀念堂」)
2. 使用 `search_attractions` 工具搜尋，列出每個景點的「預定編號 (id)」與「景點名稱 (name)」
3. 詢問「景點預定編號」、「日期 (YYYY-MM-DD)」以及「時段 (上午/下午)」
   - 價格規則：上午為 NT$2,000，下午為 NT$2,500
4. 將使用者輸入的時段與價格轉換為系統格式 (上午轉為 morning，下午轉為 afternoon)
5. 使用 `book_trip` 工具發送預定請求
6. 回傳結果中的 Booking Page 網址連結 (message 內容) 提供給使用者，請使用者點擊連結前往完成付款